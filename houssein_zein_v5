BLACK   = "\033[30m"
RED     = "\033[91m"
GREEN   = "\033[92m"
YELLOW  = "\033[93m"
BLUE    = "\033[94m"
MAGENTA = "\033[95m"
CYAN    = "\033[96m"
WHITE   = "\033[97m"
RESET   = "\033[0m"
'''
😀 → \U0001F600

😃 → \U0001F603

🙂 → \U0001F642
'''

import pandas as pd
import os

def is_file_open(file_path):
    """Return True if file is locked by another process."""
    if not os.path.exists(file_path):
        return False
    try:
        # Try opening for exclusive access
        with open(file_path, "a"):
            return False
    except PermissionError:
        return True

file_path = r"C:\private_data\Houssein\python\private_simple_codes\PythonString_Codes_Collection\fruit_table.xlsx" 

while is_file_open(file_path):
    print("⚠️   Excel file is still open, please close it to continue...\n")

    x=input("would you like force close?, enter y if yes, or close manually than click Enter\n" )
    if(str(x) == "y" or str(x) == "Y"):    
    # This kills all Excel processes (Windows)
        os.system("taskkill /f /im excel.exe")
        print("All Excel windows closed.\n")
        break
    #input("Press Enter after closing the file...")

print("File is closed ✅. Proceeding with processing...\n")

print("==========================================")

# Load existing Excel file
try:
    df = pd.read_excel(file_path)
except FileNotFoundError:
    # If the file does not exist, create a new one with headers
    df = pd.DataFrame(columns=["First Name", "Last Name", "Age", "Favorite Fruit"])


print("Hello at My simple Python Application")
print("=======================")
#Part 1: Personal Info Collector 
'''
. Create 3 variables to store
    - A person's name (string) 
    - Their age (integer) 
    - Their favorite fruit (string) 
'''

first_name=""

while not first_name.isalpha():
    first_name = input("Enter The First Name, it must be only alphabetic: \n")
    if not first_name.isalpha():
        print("Invalid username ❌ (letters only allowed)\n")

print(f"Valid username ✅: {first_name}")

print("==========================================")
last_name=""

# get lastname in alpha not digit
while not last_name.isalpha():
    last_name = input("Enter The last Name, it must be only alphabetic:\n")
    if not last_name.isalpha():
        print("Invalid lastname ❌ (letters only allowed)\n")

print(f"Valid username ✅: {last_name}")

print("==========================================")

age=0
while True:
    age = input("Enter your age, it must be between 0 and 100:\n")

    # check if input is digits only
    if age.isdigit():
        age= int(age)

        # check if age < 64
        if age < 100 and age> 0:
            print(f"Valid age:✅ {age}")
            break
        else:
            print("❌ Age must be between a and 100\n.")
    else:
        print("❌ Invalid input (digits only).\n")

print("==========================================")

# List of valid fruits
fruits = [
    "apple", "apricot", "avocado", "banana", "blackberry", "blueberry",
    "cherry", "coconut", "cranberry", "date", "dragonfruit", "durian",
    "fig", "gooseberry", "grape", "grapefruit", "guava", "jackfruit",
    "kiwi", "lemon", "lime", "lychee", "mango", "melon", "nectarine",
    "orange", "papaya", "passionfruit", "peach", "pear", "persimmon",
    "pineapple", "plum", "pomegranate", "raspberry", "strawberry",
    "tangerine", "watermelon"
]
# Ask user for input
# Fruit list

fruits = [
    "apple", "apricot", "avocado", "banana", "blackberry", "blueberry",
    "cherry", "coconut", "cranberry", "date", "dragonfruit", "durian",
    "fig", "gooseberry", "grape", "grapefruit", "guava", "jackfruit",
    "kiwi", "lemon", "lime", "lychee", "mango", "melon", "nectarine",
    "orange", "papaya", "passionfruit", "peach", "pear", "persimmon",
    "pineapple", "plum", "pomegranate", "raspberry", "strawberry",
    "tangerine", "watermelon"
]

# Show fruits as a menu
while True:
    print("Select a fruit from the list:")
    for i, fruit in enumerate(fruits, start=1):
        print(f"{i}. {fruit}")

    # Ask the user to pick a number
    choice = input("Enter the number of your fruit: ")

    # Validate choice
    if choice.isdigit():
        choice = int(choice)
        if 1 <= choice <= len(fruits):
            print(f"✅ You selected: {fruits[choice-1]}")
            break
        else:
            print("❌ Invalid number, not in the list.")
    else:
        print("❌ Please enter a number.")
print("==========================================")
print(f"😀 {RED}Thank you for using our application.{RESET}😀")
print(
    f"\t{GREEN}Your name is: {first_name} {last_name}{RESET}\n"
    f"\t{YELLOW}You are: {age} years old.{RESET}\n"
    f"\t{CYAN}And you love: {fruit}.{RESET}\n"
)

# Create a new row as dictionary
new_row = {
    "First Name": first_name,
    "Last Name": last_name,
    "Age": age,
    "Favorite Fruit": fruit,
   
}

# Append the row
df = pd.concat([df, pd.DataFrame([new_row])], ignore_index=True)

# Save back to Excel
df.to_excel(file_path, index=False)

print("✅ New employee added successfully!")
print("==========================================")

from openpyxl import load_workbook

wb = load_workbook(file_path)
ws = wb.active

from tabulate import tabulate
table_data = [[cell.value for cell in row] for row in ws.iter_rows()]
print(tabulate(table_data, tablefmt="grid"))

print("==========================================")

#3. Take the name, and:
# - Print it in uppercase 
print(f"The Uppercase Form of ({first_name}) Is:,({first_name.upper()})")
print("==========================================")
# - Print it in lowercase 
print(f"The Lowercase Form of ({first_name}) Is:,({first_name.lower()})")
print("==========================================")
# - Print only the first character 
print(f"The First Character of {first_name} Is: {first_name[0]}")
print("==========================================")
# - Print the last 3 characters using slicing 
print(f"The Last Three Character of ({first_name}) are: ({first_name[len(first_name)-3:]})")
print("Second method")
print(f"The Last Three Characters of ({first_name}) are: ({first_name[-3:]})")
print("==========================================")
#4. Use .capitalize() to make the name start with a capital letter and all the other letters lowercase. 
print(f"The first Letter of ({first_name})Is Capitalised:{first_name.title}")
print("==========================================")
#5. Count how many times the letter "a" appears in the favorite fruit. 
print(f"The {fruit} contains {fruit.count('a')} letter(s) 'a'")
print("==========================================")

#6. Split the sentence: "Python is awesome and fun" using the space character " " and print the  resulting list.
sentence="Python is awesome and fun"
print(f"the split of {sentence} is: {sentence.split(" ")}")

print("The split of the sentence by letter is:\n")
for word in sentence:
    print(word)
print("==========================================")
word=""
print("The split of the sentence by word is:",word)
sentence1=sentence.split(" ")
for word in sentence1:
    print(word)
print("==========================================")

#Part 3: Arithmetic Challenge 
#7. Assume the person spends 450000 L.L. each day. Calculate how much they spend in a week and  format the result as: 
# Total weekly spending is: 3150000 L.L 

Daily_expenses = int(input(f"Please enter how much {first_name} spends per day (in L.L.): "))
weekly_expenses = Daily_expenses * 7
print(f"Total weekly spending of {first_name} is:{YELLOW} {weekly_expenses} L.L. {RESET} per week.")
print("==========================================")

# 8. Calculate how much this amount is in dollars if 1 USD = 90000 L.L. Print it with 2 digits after the 
#decimal point using f-strings. 
weekly_expenses_dollars= weekly_expenses/90000
print(f"Total weekly spending of {first_name} is:{GREEN} {weekly_expenses_dollars:.2f} $ {RESET}per week.")
